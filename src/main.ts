// @ts-ignore
function main() { // eslint-disable-line @typescript-eslint/no-unused-vars
  try {
    console.log('This is Google Apps Script boilerplate.');
  } catch (e) {
    console.error(`failed to execute script. error: ${e}`);
  }
}
